# 🎰 今すぐ始める！クイックスタートガイド

## ⚡ 30秒で起動

```bash
cd poker-gto-analyzer
./start.sh
```

完了！ブラウザで http://localhost:5000 を開いてください。

---

## 📱 スマホからのアクセス方法

### 1. PC と スマホを同じ WiFi に接続

### 2. PC のターミナルで IP アドレスを確認

```bash
# macOS
ifconfig | grep "inet " | grep -v 127.0.0.1

# Linux
hostname -I
```

例: `192.168.1.100`

### 3. スマホで以下にアクセス

```
http://192.168.1.100:5000
```

---

## 🎯 使い方（超シンプル）

### 1️⃣ ハンドID を入力

Ten-Four で自分がプレイしたハンドを開く
→ URL から ID をコピー

例:
```
https://tenfour-poker.com/hand/J674e1buxOGyzZB15uwY
                              ^^^^^^^^^^^^^^^^^^^^^^
                            ← この部分をコピー
```

### 2️⃣ Web UI のフォームに貼り付け

```
ハンドID (1行に1つ)
J674e1buxOGyzZB15uwY
QZVSZQq4RedGPnlQZ3gs
KTFeCBYS3lIPNYyTJkRk
```

### 3️⃣ 「🔍 分析する」をクリック

待つだけで...

### 4️⃣ 結果を見る

```
✅ 分析完了 (3 ハンド)

# 🎰 GTO 対応ポーカー分析レポート

### ハンド 1: AK

**基本情報:**
- ポジション: BTN
- アクション: raise
- 結果: LOSE (-50.0bb)

**GTO 評価:**
- GTO通り ✅
- BTN からの AK raise は GTO 基準に合致しています

**文脈付きアドバイス:**
✅ **判断は正しいです**
負けていますが、AK を BTN から raise するのは GTO 戦略の標準的な判断です。
短期的な結果に左右されず、長期的には正しい判断を繰り返すことが重要です。
```

---

## 🔧 トラブルシューティング

### Q. 「接続がリセットされました」

**原因**: スマホと PC が別の WiFi に接続している

**解決**:
```bash
# 同じ WiFi に接続
# ルーター名を確認して接続
```

### Q. 「localhost は応答しません」

**原因**: サーバーが起動していない

**解決**:
```bash
# ターミナルで確認
./start.sh

# ブラウザで表示されるまで待つ
```

### Q. スマホから 192.168.x.x:5000 に接続できない

**原因**: ファイアウォール設定

**解決 (macOS)**:
```bash
# システム環境設定 → セキュリティ
# 5000 番ポートを許可
```

**解決 (Linux)**:
```bash
sudo ufw allow 5000
```

### Q. 「何が GTO 的に正解？」

**答え**: GTO (Game Theory Optimal) は、数学的に最適なプレイ方法です。

- **ポジション別に異なる**
  - BTN（最後に行動）：最も広い range で raise
  - UTG（最初に行動）：最も狭い range で raise

- **結果より判断を重視**
  - 負けても GTO 的に正解 = OK
  - 勝っても GTO 的に間違い = NG

**例**:
- AK を BTN から raise → GTO 的に正解 ✅
- 72o を UTG から raise → GTO 的に間違い ❌

---

## 💾 分析履歴

### 自動保存される

分析した結果は自動的に保存されます：

```
reports/
├── analysis_20260207_143021.md
├── analysis_20260207_150145.md
└── analysis_20260207_152030.md
```

### 履歴を見る

Web UI の **「📜 履歴」** ボタンをクリック

---

## 🚀 GitHub に自動保存する（オプション）

### GitHub Token を設定

```bash
# .env ファイルを作成
cat > poker-gto-analyzer/.env << 'EOF'
GITHUB_TOKEN=github_pat_xxxxxxxxxxxxxxxxxxxx
GITHUB_REPO=username/my-poker-analysis
EOF
```

**Token の取得方法**:
1. https://github.com/settings/tokens にアクセス
2. 「Generate new token」をクリック
3. `repo` にチェック
4. Token をコピー
5. `.env` に貼り付け

### これで...

分析結果が自動的に GitHub に push されます！

```
✅ GitHub に保存されました
```

---

## 📊 分析結果の見方

### 1. GTO 評価

| 記号 | 意味 | 対応 |
|-----|------|------|
| ✅ GTO通り | GTO 的に正解 | そのまま継続 |
| ⚠️ 非GTO | GTO から外れている | 改善が必要 |

### 2. 文脈付きアドバイス

```
✅ **判断は正しいです**
  負けていますが、判断は正解です。
  長期的には利益になります。

⚠️ **判断を見直してください**
  このハンドはこのポジションから fold するべきです。
```

### 3. 総合評価

```
## 🎯 総合評価

- **GTO 整合率:** 3/5 (60%)
- **主なリーク:** 弱いハンドを開き過ぎ
- **推奨アクション:** 72o、82o などを fold する
```

---

## 🎓 もっと学びたい人へ

### Ten-Four

https://tenfour-poker.com/ でプレイして、ハンドを記録

### GTO の基本

| ポジション | raise する手 | 数 |
|--------|----------|-----|
| BTN | AA-77, AK, AQ, KQ, AJ, KJ, QJ | 14 |
| SB | AA-66, AK, AQ, KQ | 8 |
| UTG | AA-TT, AK, AQ, AJ, KQ | 9 |

### 本を読む

- 「Poker Fundamentals」
- 「Applications of No-Limit Hold'em」

---

## ✅ 完璧！これで完成です

```
📱 スマホで ID を送信
    ↓
💻 Python が分析
    ↓
📊 結果が表示される
    ↓
📚 GitHub に履歴が保存される
```

**何か不明な点があれば、README.md や COMPLETE_GUIDE.md を参照してください！**

---

**🎰 Happy Poker Learning!**

*GTO を学んで、次のレベルへ進みましょう！*
